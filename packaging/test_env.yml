name: geosampler-test
channels:
  - conda-forge
  - defaults
dependencies:
  - pip==22.0.4
  - geopandas>=0.10
  - pyogrio
  - pygeos
  - pip:
    - -r test_requirements.txt
    - -r doc_requirements.txt